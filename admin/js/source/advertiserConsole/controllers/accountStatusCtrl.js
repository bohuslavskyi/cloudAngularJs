(function (angular) {
    'use strict';

    var common = angular.module('adConsole');

    /**
     *
     * @class userCtrl
     * @module u
     *
     */
    common.controller('accountStatusCtrl',  ['$scope','uStorage', '$location',
        function ($scope, uStorage, $location) {
            function init() {

            }

            init();

        }]);

})(angular);